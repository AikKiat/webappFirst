export default function Page() //just like java, name this (function) component after the standard file name Page, which must be named this way to define a page. 
//New route segments are created under dashboard, like a tree. 2 new branches / route segments, which is customers and invoices.
{
    return (<p>Invoices Page</p>);
}